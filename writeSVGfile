node = hou.pwd()
geo = node.geometry()

# Add code to modify contents of geo.
# Use drop down menu to select examples.

filename = node.evalParm('filename')
maxsize = node.evalParm('maxsize')
print filename, maxsize

with open(filename, 'w') as fp:
    fp.write('Hello World')
    

    
